import { DrawParams } from "./effects/DrawParams"
import { RenderingContext } from "./rendering/RenderingContext"

export abstract class Effect<T extends DrawParams = DrawParams, K extends RenderingContext = RenderingContext> {
    abstract draw(params: T): void;
    abstract context(): K;
}
